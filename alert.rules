groups:
  - name: app_alerts
    rules:
      - alert: HighErrorRate
        expr: increase(http_requests_total{status=500}[5m]) > 10
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "High error rate in the app"